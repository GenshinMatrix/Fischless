<ui:Page x:Class="Fischless.Views.PageHome"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:animations="clr-namespace:Fischless.Design.Animations;assembly=Fischless.Design"
         xmlns:controls="clr-namespace:Fischless.Design.Controls;assembly=Fischless.Design"
         xmlns:conv="clr-namespace:ValueConverters;assembly=ValueConverters"
         xmlns:converters="clr-namespace:Fischless.Design.Converters;assembly=Fischless.Design"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:dd="urn:gong-wpf-dragdrop"
         xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
         xmlns:m="clr-namespace:Fischless.Models"
         xmlns:markups="clr-namespace:Fischless.Design.Markups;assembly=Fischless.Design"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:ui="http://schemas.modernwpf.com/2019"
         xmlns:vm="clr-namespace:Fischless.ViewModels"
         d:DesignHeight="450"
         d:DesignWidth="800"
         FontSize="14"
         mc:Ignorable="d">
    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityReversedConverter" IsInverted="True" />
        <conv:ValueConverterGroup x:Key="NullToVisibilityConverter">
            <conv:NullToBoolConverter />
            <conv:BoolToVisibilityConverter />
        </conv:ValueConverterGroup>
        <conv:ValueConverterGroup x:Key="NotNullToVisibilityConverter">
            <conv:NullToBoolConverter />
            <conv:BoolToVisibilityConverter IsInverted="True" />
        </conv:ValueConverterGroup>

    </Page.Resources>
    <Grid>
        <Grid Margin="0,0,0,0"
              HorizontalAlignment="Left"
              VerticalAlignment="Bottom">
            <Image Width="418"
                   Opacity="0.3"
                   Source="/Assets/Images/UI_SummerTime_ImgDecorate_01.png" />
        </Grid>
        <Grid Margin="0,-2,5,0"
              HorizontalAlignment="Right"
              VerticalAlignment="Top">
            <Image Width="84"
                   Opacity="0.3"
                   Source="/Assets/Images/UI_Icon_GcgBossChallenges_Difficulty.png" />
        </Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="6,3,3,0">
                <StackPanel Orientation="Horizontal">
                    <Button BorderBrush="Transparent" Command="{Binding AddContactCommand}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                             FontSize="16"
                                             Glyph="{x:Static controls:FontSymbols.AddFriend}" />
                                <TextBlock Margin="5,0,0,0" Text="添加" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button Margin="8,0,0,0"
                            BorderBrush="Transparent"
                            Command="{Binding SettingsCommand}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                             FontSize="16"
                                             Glyph="{x:Static controls:FontSymbols.Settings}" />
                                <TextBlock Margin="5,0,0,0" Text="设定" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button Margin="8,0,0,0"
                            BorderBrush="Transparent"
                            Command="{Binding RefreshCommand}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                             FontSize="16"
                                             Glyph="{x:Static controls:FontSymbols.Refresh}" />
                                <TextBlock Margin="5,0,0,0" Text="刷新" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>

            <controls:SmoothScrollViewer Grid.Row="1"
                                         Margin="6,3,6,3"
                                         PanningMode="None"
                                         VerticalScrollBarVisibility="Visible">
                <ListView dd:DragDrop.DropHandler="{Binding}"
                          dd:DragDrop.IsDragSource="True"
                          dd:DragDrop.IsDropTarget="True"
                          dd:DragDrop.UseDefaultDragAdorner="True"
                          AllowDrop="True"
                          ItemsSource="{Binding Contacts}"
                          ScrollViewer.CanContentScroll="False"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Visible"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          VirtualizingPanel.IsVirtualizing="False">
                    <i:Interaction.Behaviors>
                        <controls:ListViewMouseWheelBubblingBehavior />
                    </i:Interaction.Behaviors>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel VerticalAlignment="Top">
                                <i:Interaction.Behaviors>
                                    <animations:StackPanelChildrenTransitionsDownToUpBehavior />
                                </i:Interaction.Behaviors>
                            </StackPanel>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="vm:ContactViewModel">
                            <Grid Height="50"
                                  Background="#01000000"
                                  MouseLeftButtonDown="OnContactMouseLeftButtonDown">
                                <i:Interaction.Behaviors>
                                    <animations:FrameworkElementLoadedTransitionBehavior IsOnce="True"
                                                                                         Transition="FadeIn"
                                                                                         Duration="500" />
                                </i:Interaction.Behaviors>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0"
                                      MinWidth="300"
                                      Margin="10,0,0,0"
                                      VerticalAlignment="Center"
                                      Tag="{Binding Contact.Guid}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"
                                                Margin="0,0,0,0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                        <Grid>
                                            <Image Width="45"
                                                   Height="45"
                                                   HorizontalAlignment="Left"
                                                   RenderOptions.BitmapScalingMode="Fant"
                                                   Source="{Binding Contact.LocalIconUri, Converter={x:Static converters:UriToImageSourceConverter.Instance}, ConverterParameter=45, Mode=OneWay}" />
                                        </Grid>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                Margin="15,0,0,0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Margin="0"
                                                       FontSize="18"
                                                       Text="{Binding Contact.AliasName}" />
                                            <Border Margin="3,0,0,0"
                                                    Background="#EA1D953F"
                                                    BorderBrush="AliceBlue"
                                                    BorderThickness="0"
                                                    CornerRadius="3"
                                                    Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Margin="3,0,3,0"
                                                           FontSize="16"
                                                           Foreground="#FFFFFF"
                                                           Opacity="1"
                                                           Text="正在游戏" />
                                            </Border>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0"
                                                    Orientation="Horizontal"
                                                    ToolTipService.Placement="Top"
                                                    Visibility="{Binding IsFetched, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <ToolTipService.ToolTip>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontFamily="{DynamicResource TextThemeFontFamily}" Text="{Binding Contact.Uid, StringFormat='UID:{0}'}" />
                                                </StackPanel>
                                            </ToolTipService.ToolTip>
                                            <TextBlock Margin="0"
                                                       FontSize="16"
                                                       Foreground="#A0A0A0"
                                                       Text="{Binding Contact.NickName}" />
                                            <TextBlock Margin="5,0,0,0"
                                                       VerticalAlignment="Center"
                                                       FontSize="12"
                                                       Foreground="#A0A0A0"
                                                       Text="|" />
                                            <TextBlock Margin="5,0,0,0"
                                                       FontSize="16"
                                                       Foreground="#A0A0A0"
                                                       Text="{Binding Contact.RegionName}" />
                                            <TextBlock Margin="5,0,0,0"
                                                       VerticalAlignment="Center"
                                                       FontSize="12"
                                                       Foreground="#A0A0A0"
                                                       Text="|" />
                                            <TextBlock Margin="5,0,0,0"
                                                       FontSize="16"
                                                       Foreground="#A0A0A0"
                                                       Text="Lv." />
                                            <TextBlock FontSize="16"
                                                       Foreground="#A0A0A0"
                                                       Text="{Binding Contact.Level}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                                <Grid Grid.Column="1"
                                      Margin="36,0,0,0"
                                      VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Button Margin="0"
                                                Padding="0"
                                                BorderThickness="0"
                                                Command="{Binding LazyInfo.CancelRedCommand}"
                                                CommandParameter="Lazy"
                                                Style="{StaticResource TransparentButtonStyle}"
                                                Visibility="{Binding LazyInfoFetched.IsUnlocked, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Button.Content>
                                                <Grid Width="30"
                                                      Height="30"
                                                      VerticalAlignment="Center"
                                                      Visibility="{Binding LazyInfo.IsShown, Converter={StaticResource BoolToVisibilityConverter}}">
                                                    <Grid>
                                                        <Image Width="30"
                                                               Height="30"
                                                               HorizontalAlignment="Left"
                                                               Source="/Assets/Images/UI_MarkQuest_Random_Proce.png"
                                                               Stretch="Uniform" />
                                                    </Grid>
                                                    <Image Width="15"
                                                           Margin="0,-3,-3,0"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Top"
                                                           Opacity="1"
                                                           Source="/Assets/Images/UI_Img_Red.png"
                                                           Visibility="{Binding LazyInfo.IsRed, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                                                    <Image Width="15"
                                                           Margin="0"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Bottom"
                                                           Opacity="1"
                                                           Source="/Assets/Images/UI_Tips_Item_Complete.png"
                                                           Visibility="{Binding LazyInfo.IsGreen, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                                                    <Image Width="15"
                                                           Margin="0"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Bottom"
                                                           Opacity="1"
                                                           Source="/Assets/Images/UI_Tips_Item_Warning.png"
                                                           Visibility="{Binding LazyInfo.IsYellow, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                                                </Grid>
                                            </Button.Content>
                                            <ui:FlyoutService.Flyout>
                                                <ui:Flyout>
                                                    <StackPanel TextBlock.FontFamily="{DynamicResource TextThemeFontFamily}">
                                                        <StackPanel Margin="4,0,0,0" Orientation="Horizontal">
                                                            <Grid>
                                                                <Image Width="30"
                                                                       Height="30"
                                                                       HorizontalAlignment="Left"
                                                                       Source="/Assets/Images/UI_MarkQuest_Random_Proce.png"
                                                                       Stretch="Uniform" />
                                                            </Grid>
                                                            <StackPanel Margin="12,0,0,0">
                                                                <TextBlock Text="每日世界任务" />
                                                                <ContentControl FontFamily="{DynamicResource TextThemeFontFamily}" FontSize="12">
                                                                    <ContentControl.Style>
                                                                        <Style TargetType="ContentControl">
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding LazyInfoFetched.IsFinished}" Value="False">
                                                                                    <Setter Property="Content">
                                                                                        <Setter.Value>
                                                                                            <TextBlock Foreground="{DynamicResource AccentForeground}" Text="任务未完成" />
                                                                                        </Setter.Value>
                                                                                    </Setter>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding LazyInfoFetched.IsFinished}" Value="True">
                                                                                    <Setter Property="Content">
                                                                                        <Setter.Value>
                                                                                            <TextBlock Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Text="任务已完成" />
                                                                                        </Setter.Value>
                                                                                    </Setter>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </ContentControl.Style>
                                                                </ContentControl>
                                                            </StackPanel>
                                                            <Button Margin="10,0,0,0"
                                                                    Padding="0"
                                                                    Command="{Binding LaunchLazyCommand}"
                                                                    ToolTipService.ToolTip="启动组件">
                                                                <Grid Width="30"
                                                                      Height="30"
                                                                      VerticalAlignment="Center">
                                                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                                                 FontSize="16"
                                                                                 Glyph="{x:Static controls:FontSymbols.Play}" />
                                                                </Grid>
                                                            </Button>
                                                        </StackPanel>
                                                        <Grid>
                                                            <Grid Visibility="{Binding LazyInfoFetched.IsFetched, Converter={StaticResource BoolToVisibilityReversedConverter}}">
                                                                <Image Width="150"
                                                                       Height="150"
                                                                       Source="/Assets/Images/e_tiangou.cbec2da1..png"
                                                                       Stretch="Uniform" />
                                                            </Grid>
                                                            <Grid Visibility="{Binding LazyInfoFetched.IsFetched, Converter={StaticResource BoolToVisibilityConverter}}">
                                                                <Image Width="150"
                                                                       Height="150"
                                                                       Source="/Assets/Images/stage_3_icon.02bbe66b..png"
                                                                       Stretch="Uniform" />
                                                            </Grid>
                                                        </Grid>
                                                    </StackPanel>
                                                </ui:Flyout>
                                            </ui:FlyoutService.Flyout>
                                            <i:Interaction.Behaviors>
                                                <controls:ButtonFlyoutCursorBehavior Cursor="{Binding Cursor, RelativeSource={RelativeSource AncestorType=Window}}" />
                                            </i:Interaction.Behaviors>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ContextMenu>
                        <ContextMenu Cursor="{DynamicResource ThemeCursor}">
                            <MenuItem Command="{Binding LaunchGameFromListCommand}" Header="启动游戏">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Play}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="{Binding LaunchGameFromListWithDelayCommand}"
                                      Header="缓启动游戏"
                                      ToolTip="3分钟后启动选中账号">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Play}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="{Binding CancelLaunchGameFromListWithDelayCommand}"
                                      Header="取消缓启动游戏"
                                      Visibility="{Binding CancelLaunchGameDelayTokenSource, Converter={StaticResource NotNullToVisibilityConverter}}">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Cancel}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator />
                            <MenuItem Command="{Binding RefreshContactCommand}" Header="刷新">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Refresh}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="{Binding CopyUidCommand}" Header="复制 UID">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Copy}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="{Binding CopyCookieCommand}" Header="复制 Cookie">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Copy}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator />
                            <MenuItem Command="{Binding EditContactCommand}" Header="编辑">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Edit}" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="{Binding DeleteContactCommand}" Header="删除">
                                <MenuItem.Icon>
                                    <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                 FontSize="16"
                                                 Glyph="{x:Static controls:FontSymbols.Delete}" />
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </ListView.ContextMenu>
                </ListView>
            </controls:SmoothScrollViewer>
        </Grid>
    </Grid>
</ui:Page>
